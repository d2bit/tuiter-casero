<div class='tweet-box col-sm-12 col-md-6'>
  <div class='thumbnail'>
    <div class='caption'>
      <p>
        <%= tweet.text %>
      </p>
    </div>
    <% tweet.media.each do |media| %>
      <%= image_tag(media.media_url, class: 'image-responsive') %>
    <% end %>
  </div>
</div>
